# Preamble

This file only template directives; it is skipped for the actual output.

{{skip "__preamble"}}

{{if not .landingzone_to_raw}}
  {{skip "jobs/{{.job_name}}/landingzone_to_raw.py"}}
{{end}}

{{if not .raw_to_trusted}}
  {{skip "jobs/{{.job_name}}/raw_to_trusted.py"}}
{{end}}
