{
  "properties": {
    "job_name": {
      "type": "string",
      "default": "job_name",
      "description": "Job name",
      "order": 1
    },
    "landingzone_to_raw": {
      "order": 2,
      "type": "boolean",
      "description": "Does this job move data from landinzone (e.g. json file) to raw delta table?",
      "default": true
    },
    "raw_to_trusted": {
      "order": 3,
      "type": "boolean",
      "description": "Does this job move data from raw delta table to a trusted table?",
      "default": true
    },
    "use_serverless": {
      "order": 4,
      "type": "boolean",
      "description": "Use serverless compute? Otherwise a pre-defined cluster will be used.",
      "default": false
    }
  }
}
